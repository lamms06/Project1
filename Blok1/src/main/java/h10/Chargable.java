package h10;

// Create an interface Chargeable with a method int charge(int amount). With this method, a certain
//        amount of energy can be injected into the object. It returns the new amount of energy.


public interface Chargable {

    int charge(int amount);

}
